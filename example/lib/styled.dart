import 'package:flutter/material.dart';
import 'package:styled_widget/styled_widget.dart';

void build(Widget widget) {
  // widget.translate(offset: offset);
  widget.scale(origin: Offset(1, 1));
  // widget
}
